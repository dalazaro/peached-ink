<h1><%= @collection.name %></h1>
<h2><%= @collection.description %></h2>

<%= button_to "Edit", edit_collection_path(@collection), method: :get %>

<%= button_to "Delete", "/collections/#{@collection.id}", method: :delete %>
